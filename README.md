# AI Food Order Agent 🍜

[中文版本](README.zh.md) | [English version](README.md)

An experimental “AI restaurant agent” that combines vector‑based intent recognition, LangGraph workflow orchestration, and RAG‑style retrieval to drive an automated food‑ordering conversation.

## 🧩 Architecture

- **LangGraph‑driven state machine**  
  The conversation is modelled as a `StateGraph` (see `order_agent.py`).  
  Each node represents a logical step (intent recognition, menu search, order processing, price/address confirmation, response generation, etc.) and edges route based on intent or dialog stage.

- **AI Agent / LLM integration**  
  - `llm_chater.SimpleLLM` wraps a HuggingFace causal‑LM pipeline and provides context‑aware chat, intent analysis and ad‑hoc extraction helpers.  
  - The agent can “escalate” to the LLM when the vector intent recognizer is uncertain (see `llm_intent_recognition()` and `intent_recognition_node`).

- **RAG & Vector Search**  
  - Menu items are indexed in a vector store (`vector_store`) for semantic search.  
  - When users mention a dish that isn’t an exact match, the agent uses the vector DB to resolve the closest item.  
  - The same vector index underpins the intent recognizer (`IntentRecognizer` in `IntentRecognizer.py`).

- **Intent recognition**  
  - Vectors + lightweight classifiers identify intents such as `order`, `search`, `confirm_price`, etc.  
  - The `intention/intentions_enum.py` enum defines all possible intents and includes utilities (e.g. `from_str`).  
  - Low‑confidence cases automatically trigger a clarification node or an LLM fallback.

### Agent Workflow Diagram

![Agent Workflow](docs/images/agent-workflow.png)

The graph above illustrates a complete single round of conversation dialogue flow:
1. **intent_recognition** analyzes user input and routes to appropriate handlers
2. Nodes like **process_order**, **search_menu**, **confirm_price**, etc. handle specific intents
3. **handle_clarification** manages low-confidence cases
4. All paths converge at **generate_response** to produce the final reply
5. The workflow ends after response generation

## 📁 Key Components

| Module/Folder | Purpose |
|--------------|---------|
| `order_agent.py` | Core agent logic & state graph |
| `menu.py` | Menu data model and price lookup |
| `Customer`, `order` classes | Order/customer helpers |
| `IntentRecognizer.py` | Vector-based intent classifier |
| `NLPEntityExtractor.py` | Finds dish names & quantities in input |
| `FAISSMenuStore.py` | Vector index for menu items |
| `llm_chater.py` | LLM wrapper for chat & intent analysis |
| `stage_enum.py` | Conversation stages enumeration |
| `intention/intentions_enum.py` | Intent definitions |

## ⚙️ Usage Scenario

1. **User speaks** (e.g. “I’d like a spicy kung pao chicken”).  
2. Agent calls `intent_recognizer.recognize()`.  
3. Graph routes to `process_order_node` or `search_menu_node`.  
4. If order, entities are extracted; menu lookup occurs (possibly via vector DB).  
5. Order state updates; stage machine may transition to price/address confirmation.  
6. Responses are generated by `_generate_stage_response()` or LLM if necessary.

## 🧠 AI‑centric Features

- **Hybrid intent handling** – vector retrieval with LLM recovery.  
- **RAG‑style menu lookup** – semantic similarity for fuzzy dish names.  
- **Context‑aware LLM dialogue** via `SimpleLLM` (stores a limited history).  
- **Dynamic clarification questions** on low‑confidence detection.

## 📦 Installation & Dependencies

> (Assumed the environment is managed via Anaconda/venv; install Transformers, LangChain, PyTorch, etc.)

```sh
# create/activate environment
pip install -r requirements.txt
```

## 🛠 Extending the Project

- Add new intents in `intentions_enum.py` and update routing.  
- Swap or retrain the vector intent recognizer.  
- Replace the LLM backend (e.g. use GPT‑4/other HF models).  
- Plug in a real order API instead of `print()`/`input()` stubs.

---

> This project demonstrates how an AI‑driven conversational workflow can be constructed with modern tools like LangGraph, RAG, and vector‑based intent recognition for practical tasks such as restaurant ordering.  
Feel free to fork, experiment, and build on it!